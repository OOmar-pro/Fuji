<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light Mode -->
    <style name="LightMode" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="actionBarTheme">@style/ActionBarLight</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="l">true</item>
        <!-- Customize your theme here. -->
        <item name="colorBackground">@color/gray_light</item>
        <item name="colorText">@color/gray</item>
    </style>

    <!-- ActionBar Light Mode -->
    <style name="ActionBarLight" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- title text color -->
        <item name="android:textColorPrimary">@color/black</item>
        <!-- subtitle text color -->
        <item name="android:textColorSecondary">?android:textColorPrimary</item>
        <!-- action menu item text color -->
        <item name="actionMenuTextColor">?android:textColorPrimary</item>
        <!-- action menu item icon color -->
        <item name="colorControlNormal">?android:textColorPrimary</item>
    </style>

    <style name="SettingsFragmentStyleWhite">
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="colorControlNormal">@color/blue</item>
    </style>


    <!-- Dark Mode -->
    <style name="DarkMode" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="actionBarTheme">@style/ActionBarDark</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black_light</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
        <item name="colorBackground">@color/black_medium</item>
        <item name="colorText">@color/white</item>
    </style>

    <!-- ActionBar Dark Mode -->
    <style name="ActionBarDark" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black_light</item>
        <!-- subtitle text color -->
        <item name="android:textColorSecondary">?android:textColorPrimary</item>
        <!-- title text color -->
        <item name="android:textColorPrimary">@color/white</item>
        <!-- action menu item text color -->
        <item name="actionMenuTextColor">?android:textColorPrimary</item>
        <!-- action menu item icon color -->
        <item name="colorControlNormal">?android:textColorPrimary</item>
    </style>

    <style name="SettingsFragmentStyleDark">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="colorControlNormal">@color/blue</item>
    </style>

</resources>