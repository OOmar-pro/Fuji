<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fuji">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_fuji"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_fuji_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fuji">
        <activity
            android:name=".ui.sources.SourceListMangaActivity"
            android:exported="false" />
        <activity
            android:name=".ui.sources.SourceInfoActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>